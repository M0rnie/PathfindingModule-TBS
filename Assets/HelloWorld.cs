using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Debug.Log — это команда, которая печатает сообщение в консоль Unity.
        // Это как "print" в других языках, очень полезно для отладки.
        Debug.Log("Привет, Мир! Мой первый скрипт работает.");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
